{
  "name": "feathers-micro",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev:cars": "cd workspace/cars && yarn && yarn dev",
    "dev:bikes": "cd workspace/bikes && yarn && yarn dev",
    "dev:users": "cd workspace/users && yarn && yarn dev",
    "dev": "npm-run-all --parallel dev:**"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}